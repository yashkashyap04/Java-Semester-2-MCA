ConnectFourGame.java
    main(){
        --> prints board
        --> unless a winner is not found, loops the game through
    }

User.java --> A class for the the players playing. 2 objects of the class will be created, each for a user.
    variable:= name
    variable:= color
    method:= setName
    method:= setColor
    method:= makeMove

Board.java --> Class for the connect 4 board. 